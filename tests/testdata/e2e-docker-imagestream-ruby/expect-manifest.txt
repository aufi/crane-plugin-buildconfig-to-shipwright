# Assertions on the converted Shipwright Build manifest (crane transform output).
#
# One fixed-string pattern per line. ${VAR} references are expanded against the
# case.env values before matching. A leading '!' asserts the pattern is ABSENT.
# Blank lines and '#' comments are ignored.

kind: Build
shipwright.io/v1beta1
name: ${BUILD_STRATEGY}
kind: ClusterBuildStrategy
dockerfile
value: Dockerfile
# Output image uses the ImageStreamTag name (ruby-hello-world), not the BuildConfig name.
image: ${REGISTRY}/${NAMESPACE}/ruby-hello-world:latest
registries-insecure
github.com/openshift/ruby-hello-world
revision: master
crane.konveyor.io/converted-from
