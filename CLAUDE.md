# CLAUDE.md

This repo keeps its agent instructions in `AGENTS.md` (build steps, testing,
related repos, and the commit policy). Claude Code auto-loads `CLAUDE.md` but not
`AGENTS.md`, so this file imports it to keep one source of truth:

@AGENTS.md
